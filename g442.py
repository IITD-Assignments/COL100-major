def max_value(V, S):
    if len(V) == 1:
        return V[0]
    elif len(V) == 2:
        return max(V[0], V[1])
    elif len(V) == 3:
        pass
        # didn't write rest of the code

def max_value_indices(V, S):
    pass
